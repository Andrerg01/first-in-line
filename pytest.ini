[pytest]
testpaths = tests
# '.' lets tests import as backend.app.xxx and worker.app.xxx (no collision).
# 'backend' lets backend's own internal imports (from app.routers...) resolve.
pythonpath = . backend
